version: '3'
services:
  web:
    build: .
    command: bash -c "rm -f tmp/pids/server.pid && bundle exec rails s -p 3000 -b '0.0.0.0'"
    volumes:
      - .:/ndh-query-client
    ports:
      - "3000:3000"
    environment:
      DEFAULT_FHIR_SERVER_URL: https://ndh-server.fast.hl7.org/fhir
